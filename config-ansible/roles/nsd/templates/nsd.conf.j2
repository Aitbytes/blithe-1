server:
    server-count: 1
    ip-address: 0.0.0.0
    port: 53
    verbosity: 1
    hide-version: yes
    identity: ""
    zonesdir: "/zones"

{% for zone in nsd_zones %}
zone:
    name: "{{ zone.name }}"
    zonefile: "{{ zone.zonefile }}"
{% endfor %}

